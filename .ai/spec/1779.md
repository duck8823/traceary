# #1779: remaining payloadCodecIdentity call sites

## Policy

- Bundle import, archive restore, dedupe restore, and raw-body recovery adopt `encodeCanonicalPayload` + `storedBodyArg`. Interchange stays plaintext; the writer on the way in matches native `insertEventAndAudit`.
- Retention markers stay `payloadCodecIdentity`. Apply/verify compare stored TEXT to `EventBodyUnavailableRetentionMarker`.
- Do not change bundle or archive formats.

## I/O

- Tests drive real import / archive restore / dedupe restore on a scratch store: compressible plaintext lands as `zstd` and reads back byte-identical.

## Non-goals

- Changing interchange formats.
- Live-store measurement of #1620.
