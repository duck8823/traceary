# #2157 Codex local-path marketplace headless refresh docs

Parent: #2188. Closes only #2157.

## Structure-Behavior Design Note

Risk: **Low** — docs-only; operator refresh path.

### Requirement summary
- Headless refresh must describe both git-clone and local-path marketplace roots.
- Identify the root with `codex plugin marketplace list`.
- Local-path: sync the checkout (`git -C <root> checkout vX.Y.Z`) then `codex plugin add traceary@traceary-marketplace`.
- "No configured Git marketplaces" stays expected for local-path roots.

### Behavior tests
- `integrations verify` fails if either language guide omits the local-path sequence tokens.
