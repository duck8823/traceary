# #1743 — measure whether a body side table is worth it

Decision: **do not extract**. Public write-up: `docs/research/body-side-table-locality.md`.

Gate (warm p50, LIMIT 5000): method valid if inline/projection ≥ 2.0; material
if that holds, no table scan, inline/side ≥ 2.0, side/projection ≤ 1.5.

Scratch 512×8KiB (2026-08-15): entropy 1.87×, repetitive 1.07× after codec,
side ≈ projection, resident bytes grew. Live 2026-08-10 (cited): 2.7× warm /
15× cold is why the projection stays, not why we move bodies.
