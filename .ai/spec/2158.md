# #2158 Claude headless plugin@marketplace update docs

Parent: #2188. Closes only #2158.

## Structure-Behavior Design Note

Risk: **Low** — docs-only; operator refresh path.

### Requirement summary
- Document headless `claude plugin marketplace update` then `claude plugin update traceary@traceary-plugins` for user and `--scope local`.
- Bare `claude plugin update traceary` is documented as failing.
- Keep slash-command interactive alternative. Restart, then `traceary doctor`.
- Multi-version cache: resumed sessions may keep old snapshot hooks until fully restarted.

### Behavior tests
- `integrations verify` fails if either language guide omits `traceary@traceary-plugins`.
