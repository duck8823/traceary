# #1686 — keep the event metadata projection

Decision: **do not retire**. Public write-up:
`docs/research/event-metadata-projection-retention.md`.

Depends on #1743 (no body side table). Live 2026-08-10 2.7× / 15× still
applies. Stage 1 (read `events`, keep writing the projection) would regress
every metadata scan for ~0.47 GiB the #1620 sheet does not need.
