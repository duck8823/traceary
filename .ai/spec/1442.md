# Issue #1442: v0.31.0 release preparation

## Requirements

- Publish the completed v0.31 retention work only after copied-store dogfood and release-blocking follow-up #1486 are complete.
- Keep parent release issue #1426 open until the tagged workflow publishes the release.
- Keep every versioned manifest, landing marker, and bilingual changelog synchronized at `0.31.0`.
- Prove the new v0.31 raw-body and file-capacity workflows remain manual, opt-in, recoverable, and free of implicit plan application; the pre-existing automatic archive-before-GC path stays separately opt-in and disabled by default.

## Observable release behavior

1. Manifest and changelog verifiers accept `0.31.0` across all packages and both changelogs.
2. Full Go tests and lint pass on the release-preparation head.
3. Retention dogfood documents wrong-confirmation immutability, retry convergence, recovery, integrity, and the fixed unsafe-root warning.
4. Snapshot/release packaging succeeds without changing the only live store copy.
5. The tagged workflow publishes artifacts, Pages, and a Homebrew formula update; the installed binary reports `0.31.0`.

## Validation plan

- Run release bump and all documented release verifiers.
- Run focused retention, cockpit, full test, vet, lint, and cross-platform compile checks.
- Preview or structurally verify the landing page and run a local snapshot package build.
- Obtain an independent review on the final head, then require CI success before merge and tag.

## Self-review and rollback

- This PR changes release metadata and documentation only; implementation behavior was reviewed in the issue-specific PRs.
- If any verifier, review, QA, or workflow fails, do not tag. Fix through a scoped follow-up or update this release-preparation branch and re-run the affected gates.
- A published release is not complete until GitHub assets, Pages, Homebrew, installed version, parent issue closure, and a clean synchronized `main` are verified.
