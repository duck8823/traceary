# v0.34.0 — Consolidate and reuse: the two pillars

Supersedes `docs/architecture/hot-cold-segmented-history.md` (+ `.ja.md`), which is removed by #1671.

## Structure-Behavior Design Note

### Requirement summary

**目的.** Traceary を二つの柱に絞り、その両方が確実に・自動的に働く状態にする。

1. **記録** — 取ることはできている。会話全体を持ち続けると肥大するので、**まとめ・圧縮・索引からの退避**を行う。
2. **記憶** — 上と重なる（まとめ・圧縮）が、加えて**そのまとめの自動利用**を行う。

この二つ目が「AI も睡眠をとった方がいい」の実装にあたる。生物の睡眠と同じく、**固定化は本人が意識してやらない**、**起床時には選別が終わっている**、**忘却は機能である**。

**現状（2026-08-07 実測、live store 24.71 GiB / 534,086 events / 26,728 sessions / 2.5 か月）.**

| | |
|---|---|
| 取る | 動いている。99.99% が hook 経由 |
| 肥大化 | +9.9 GiB/月。削減機構が 5 つあり全部オフ |
| 索引 | 15.83 GiB（全体の 64.1%）。全期間に trigram を張っている |
| 重複 | `events.body` 5.05 GiB のうち **2.91 GiB (57.7%) が完全重複** |
| まとめ | セッション要約が 26,728 中 **4 件** |
| 自動利用 | **配線ゼロ**。`additionalContext` / `hookSpecificOutput` はコードベースに 1 件も無い |

**期待する振る舞い.**

- 会話が溜まったら、**その場で**（素材がエージェントのコンテキストにあるうちに）まとまる
- 起床時には、**まとまったものだけ**が手元にある。生ログは渡さない
- 古い記録は索引から退避するが、**セッション単位では引ける**
- 本文を捨てても、**いつ・何を・何回**は永久に残る。**なぜ**はエージェントが実際に畳んだ範囲にだけ残り、機械的な gc 要約（`degraded=1`）に落ちた範囲では失われる

**非対象.**

- 外部ストレージへの分離（cold DB / segment file）。撤回済み
- 失敗捕捉（exit code）。ホスト側が payload に出さない
- コスト算出。`docs/research/ai-usage-source-verification.md` で明示的に見送り済み

---

### Conceptual model

| Concept | State | Behavior | Constraint / Invariant |
|---|---|---|---|
| **Event skeleton** | id, kind, session, workspace, agent, 時刻, `body_bytes`, `body_state` | 記録時に 1 度書かれる。**不変**: id / kind / session / workspace / agent / 時刻 / `body_bytes`。**可変**: `body_state` のみ（定義済み遷移に限る） | **永久**。行は削除されない。統計は不変フィールドだけから計算する |
| **Event body** | 本文。`event_bodies` に別置 | `body_state`: `present` → `discarded`（非 attested・畳み済み）／ `compressed`（attested） | attested な本文は削除されない。非 attested は畳まれるまで削除できない。`body_state` はこの遷移以外では動かない |
| **Attested record** | 人間の指示 (`prompt`) と実行したコマンドの同一性 (`command_text`, `input_text`) | 追記のみ | **永久**（圧縮のみ）。何を指示され、何を実行したかは消えない |
| **Command output** | 実行結果。`command_outputs` に別置 | 非 attested として扱う | 「何を実行したか」の証明に出力は要らない |
| **Refinement pressure** | セッションごとの未精製バイト数 | 各 stop で機械的に加算 | LLM を使わない。DB 更新 1 行 |
| **Session refinement** | セッション要約。世代・被覆範囲・生成者を持つ | 通常は閾値超過時に**その場で**エージェントが書く（既存要約があれば**再まとめ**）。orphan range については gc が機械要約を書き `degraded=1` を立てる | **永久**。本文の約 1/500。これが残ることが本文を捨てられる条件 |
| **Recent index window** | 直近の生本文に張った trigram 索引 | 索引ファミリのバイト予算で押し出す | 予算は**索引ファミリ合計**で表現する（入力本文量ではない） |
| **Session tier index** | 全期間のセッション要約・キーワードに張った unicode61 索引 | 精製のたびに更新 | **永久**。「あのセッション」を特定できれば十分 |
| **Orphan range** | もはやエージェントが畳めない未精製範囲（compact を跨いだ、またはセッションが終了した） | compact marker で前倒し記録するか、終了 / stale 判定時に `covers_to` との差分から gc が発見する | 大きさは**典型的には**閾値未満（設計目標であり上限ではない）。gc で機械要約に落とす（参照時に畳まない）。入力が閾値を超えても扱えること |

---

### Responsibility assignment

| Responsibility | Owner | Reason to change | Not owner / reason |
|---|---|---|---|
| 何を記録するか | hook runtime | ホストの契約が変わったとき | 使用側は関与しない |
| いつ畳むべきか | stop hook の機械判断 | 閾値・圧力の定義が変わったとき | **LLM は判定に関与しない**（コスト） |
| 何を書くか（要約の中身） | エージェント（LLM） | — | Traceary は LLM を呼ばない（local-first） |
| 畳めなくなった範囲の要約 | store gc（機械要約、`degraded=1`） | 機械要約の項目が変わったとき | LLM を使わないので「なぜ」は書けない |
| 要約の保存と再まとめ | `session_refinements` + write port | 世代管理が変わったとき | hook は書式を知らない |
| 起床時に何を渡すか | 起床注入器 | 予算・選別規則が変わったとき | **選別は固定化時に済ませる**。起床時に判断しない |
| 索引に載せるか | search projection | 予算・トークナイザが変わったとき | 本文の生存とは独立 |
| 本文を捨てるか | `store gc` | 保持方針が変わったとき | 索引の退避とは独立の判断 |

---

### Boundaries / interfaces

| Boundary | Consumer | Hidden detail | Error contract |
|---|---|---|---|
| `stop` hook 終了コード | ホスト | 圧力計算・閾値 | 判定に失敗したら **exit 0**（作業を止めない） |
| 起床注入 (`additionalContext`) | ホスト | 要約の作り方・選別 | 要約が無ければ**何も出さない**（空文字を注入しない） |
| `traceary session refine` | エージェント | 世代・被覆範囲の管理。**テキストは合成しない**（渡された要約を保存し、generation / coverage だけを所有する） | 冪等。同じ範囲を二重に畳んでも要約は 1 つ |
| `event_bodies` / `command_outputs` | 読み取り経路 | 圧縮コーデック | 本文欠落は `body_state` で区別（エラーにしない） |
| 索引予算 | 運用者 | trigram 増幅率 | **索引ファミリ合計バイト**で指定する |

---

## ホスト契約（一次情報で確認、2026-08-07）

| ホスト | 起床時に注入 | stop で追加作業を依頼 | compact で依頼 | イベント占有 |
|---|---|---|---|---|
| Claude Code | ● `SessionStart` additionalContext | ● exit 2 →「continues the conversation」 | ✕ PreCompact は additionalContext 非対応 / PostCompact は「No decision control」 | 6.8% |
| Codex CLI | ● `SessionStart` stdout / additionalContext | ● `{"decision":"block","reason":...}` → continuation prompt | ✕ 注入不可 | 39.9% |
| Kimi Code | ✕ SessionStart は fire-and-forget → **初回 `UserPromptSubmit`** を使う | ● exit 2 →「a message can be appended to let the model continue」 | ✕「return values are completely ignored」 | 38.0% |
| Grok Build | ● SessionStart | ● Stop | ✕ | 2.4% |
| Antigravity | ● PreInvocation（`injectSteps.ephemeralMessage`、#1714 で実装） | ● Stop | **✕ compact hook 自体が無い** | 1.2% |
| Gemini CLI | ● SessionStart | ● AfterAgent | ✕ | ~0% |

**結論.**

- **compact は、hook を持つホストでも観測専用**。「compact 時に LLM を呼ぶ」は実装できない。梃子は **stop だけ**
- compact marker は「取り残し範囲」を**前倒しで**記録するために使う（唯一の情報源ではない — 取り残し範囲を参照）
- Claude の PostCompact 本文（実測 186 件）は無料の素材として併用する
- ホスト差は 3 つ: **Kimi の起床注入**（SessionStart → 初回 UserPromptSubmit）、**Antigravity の起床注入**（PreInvocation — #1714）、**Antigravity の compact 非観測**（hook 自体が無い）

出典: [Claude Code hooks](https://code.claude.com/docs/en/hooks) / [Codex CLI hooks](https://learn.chatgpt.com/docs/hooks) / [Kimi Code CLI hooks](https://www.kimi.com/code/docs/en/kimi-code-cli/customization/hooks.html)

---

## 睡眠サイクル

```
起きている間          hook が全部取る                          （動いている）
                          │
まとまってきたら      stop で機械判断（無料・毎ターン 2,811 回/日）
                      pressure >= 閾値 → exit 2 / decision:block
                      「今のセッションを畳んで書き込め」
                      ※ 素材はエージェントの頭の中にある。追加入力ゼロ
                      ※ 既存要約があるときは reason payload に
                        previous summary と covers_to を載せる
                          │
                      エージェントが（再）まとめを書き、
                      session_refinements へ refine で保存
                          │
寝ている間            store gc
                      索引から退避 / 非attested 本文を破棄 / attested を圧縮
                          │
起きるとき            ホスト固有の注入点に完成済み要約だけを注入
                      （Claude / Codex / Grok / Gemini は SessionStart、
                        Kimi は初回 UserPromptSubmit。
                        Antigravity の PreInvocation は #1714）
                      ※ 生ログは絶対に渡さない
```

**再まとめの経路.** Traceary は LLM を呼ばない。再まとめのテキストはエージェントが書き、`traceary session refine` の write port は渡された内容を保存し generation / coverage だけを所有する（テキストは合成しない）。そのためエージェントが書く前に既存要約が手元に要る。既存要約と `covers_to` は、畳み依頼と同じ **stop hook の reason payload** で渡す。別の読み取りコマンドは足さない。

**閾値の意味.** 閾値は「LLM 呼び出し回数」ではなく、**stop でエージェントに提示する未精製素材量の設計目標**である。畳めなくなった範囲を後から畳み直す費用ではない — 更新後の規則ではエージェントはその範囲をもう畳まない（gc の機械要約に落ちる）。orphan range の大きさは典型的には閾値未満に収まるが、**ハードな上限ではない**（下の二経路では超えうる）。

この費用は**畳みを依頼する時点でエージェントの文脈が占められる量**であって、起床時の注入量ではない。起床時に入るのは完成した要約だけで、その量は閾値と無関係に自前の予算で決まる。

| 閾値 | 畳む時点の素材量 | 判断 |
|---|---|---|
| **64 KiB** | 約 16k トークン | **採用**（設計目標） |
| 256 KiB | 約 64k トークン | 畳みを頼む時点でエージェントの文脈を占めすぎる |
| 1 MiB | 約 250k トークン | 論外 |

呼び出し回数は、重複（96.6%）を潰した後のユニーク transcript が 2.5 か月で 145 MiB なので問題にならない。

**閾値を超えうる経路.**

1. **依頼の無視.** stop トリガーは繰り返し要求の抑止なしで出荷する（Risks / rollback 参照）。エージェントが畳まないまま圧力が閾値上に残り、応じる前に compact が来ると orphan range は閾値を超える
2. **単一 oversized turn.** 圧力は stop で評価するため、1 ターンで閾値を大きく超える記録（大きな tool output・長い transcript）が、評価点を挟まずに圧力を閾値下から大きく上へ押し上げる

これらから従う帰結:

- **gc の機械要約は入力が有界だと仮定してはならない** — 閾値を超える range も扱えること
- **起床時の注入予算は閾値から推論せず、独自に enforce すること**

---

## データ層

### テーブル

| テーブル | 持つもの | 寿命 | 索引 |
|---|---|---|---|
| `events` | skeleton のみ + `body_bytes` + `body_state` | **永久** | 現行 events 索引セット |
| `event_bodies` | `event_id`, `codec`, `payload` | 条件付き | PK のみ |
| `command_audits` | 実行の同一性: `command_text`, `input_text`, 結果コード | **永久** | `event_id` |
| `command_outputs` | `event_id`, `codec`, `payload` | 条件付き | PK のみ |
| `session_refinements` | `session_id`, `generation`, `covers_from`/`covers_to`, `summary`, `keywords`, `produced_by`, `produced_at`, `degraded` | **永久** | `session_id` |
| `session_orphan_ranges` | `session_id`, `from`/`to`, `observed_at` | 精製されるまで | `session_id` |
| `search_projection_recent_documents` + `_fts` | 直近窓の生本文 | **窓** | **trigram** |
| `search_projection_session_summaries` + `_keywords` | 全期間の要約 | **永久** | **unicode61 + porter** |
| ~~`event_metadata_projection`~~ | — | **廃止** | — |

### なぜ本文を別テーブルに出すのか

本文を `events` の列に持ったままだと、破棄が `UPDATE body=''` になる。SQLite はページを解放しないのでファイルは縮まず、縮めるには VACUUM（24 GB なら同容量の空きと数十分）が要る。別テーブルにして DELETE すればページが free list に入り、次の書き込みで再利用される。**VACUUM が不要になる**のが分ける理由。

副次的に、`events` が細くなると `event_metadata_projection` の存在理由が消える。あれは「本文を持つ太いテーブルを走査したくない」ための複製で、索引名まで一対一に対応している（実測）。

```
idx_events_workspace_session_created_at_norm_id_desc          0.10 GiB
idx_event_metadata_workspace_session_created_at_norm_id_desc  0.10 GiB   ← 同じもの
```

廃止で 0.47 GiB と索引の二重管理が消える。

### 索引を 2 層にする

| 層 | トークナイザ | 対象 | コスト |
|---|---|---|---|
| 直近（予算で縛る） | **trigram** | 生本文 | 本文の 2.16 倍（実測） |
| 全期間（永久） | **unicode61 + porter** | セッション要約・キーワード | 本文の約 1/500 |

現状は全期間に trigram を張っており、これが 15.83 GiB の正体。

**予算は索引ファミリ合計で表現する。** 現行の `recent_byte_limit` は `search_projection_recent_documents` の本文バイトを縛っており（`search_projection_rebuild.go:426`）、2 GiB 指定が 6.35 GiB の索引になる。運用者が見たいのは索引の総量なので、意味を合わせる。

### 一つの本文が辿る道

```
Day 0  ターン終了
  events        kind=transcript / 時刻 / bytes=42KB / state=present
  event_bodies  42KB の本文
  直近窓        trigram 索引に載る            → 「あの文字列」で全文検索できる

Day 0  stop hook が閾値超過を検出
  エージェントがその場で畳む → session_refinements
  この本文は「畳み済み範囲」に入る

Day 7  索引予算を超えて押し出される
  直近窓から退避                              → 全文検索では出ない
  セッション要約層では引ける                  → 「あのセッション」は見つかる
  events / event_bodies はそのまま            → 開けば中身は全部ある

Day 30 store gc
  event_bodies から DELETE
  ↓ 残るもの
  events               bytes=42KB, state=discarded   → 統計は永久
  session_refinements  何をしたか                    → 永久
  要約索引             あのセッションを引ける        → 永久
```

attested は別の道を通る。

```
prompt / command_text / input_text
  Day 7   索引からは退避する（検索の話）
  Day 30  gc で zstd 圧縮、state=compressed
  永久    削除されない（証跡の話）
```

### 三つの分離

同じ操作に混ざっている三つを、別々のイベントにする。

| 分離 | いつ | 失うもの | 残るもの |
|---|---|---|---|
| **索引から退避** | 窓の予算を超えたとき | 全文検索 | セッション単位の検索・本文・統計 |
| **本文を破棄** | 畳み済み かつ 非 attested | 中身 | バイト数・時刻・件数 |
| **圧縮** | attested | 何も | 全部 |

### 消えないもの / 消えるもの

| 何 | gc で消えるか |
|---|---|
| `events` skeleton（時刻・種類・バイト数・件数） | **消えない** |
| `session_refinements`（セッション要約） | **消えない** |
| セッション要約の索引 | **消えない** |
| `prompt` 本文（人間の指示） | **消えない**（圧縮のみ） |
| `command_text` / `input_text`（実行したコマンド） | **消えない**（圧縮のみ） |
| `transcript` 本文（エージェントの発言） | 畳んだ後に消える |
| `output_text`（コマンド出力） | 畳んだ後に消える |
| trigram 全文索引 | 窓から押し出されたら消える（本文は残る） |

**捨てるのはエージェントの喋りとコマンド出力だけ**で、「いつ・誰が・何を・何回」は残る。**なぜ**はエージェントが実際に畳んだ範囲にだけ残り、機械的な gc 要約（`degraded=1`）に落ちた範囲では失われる。

### 取り残し範囲

```
stop(畳む) ── やりとり ── stop(スルー) ── compact
             └────── 畳まれていない ──────┘
```

ここで失われるのはデータではない（hook は毎ターン記録済み）。失われるのは「安く畳める機会」だけ。orphan range の大きさは**典型的には**閾値未満だが、**定義上の上限ではない**（依頼の無視・単一 oversized turn — 閾値の意味を参照）。

扱い方は一つ。**gc で機械要約に落とす** — LLM 無しで「いつ・何を・何回・どのコマンド」は作れる。`degraded=1` を立てる。「なぜ」は失われる。入力が閾値を超えても機械要約はそれを前提に扱わねばならない。

**compact で所有が移る**（2026-08-08 決定）。畳みを依頼できるのは素材がエージェントの文脈にある間だけである。compact を通過した時点で未精製の範囲は文脈から外れるので、以後どれだけ stop を繰り返しても**その範囲はもう畳めない**。したがって compact 到達時に、未精製範囲は stop 圧力から切り離して orphan range として記録し、**圧力をリセットする**。以後の stop が見るのは compact 後の素材だけであり、切り離された範囲は gc の機械要約が引き取る。この境界を引かないと、二度と応じられない依頼を毎 stop 繰り返すことになる。

**Antigravity には引けない**（残存リスク）。この境界は compact hook の観測に依存する。ホスト契約表のとおり Antigravity には compact hook が無いので、境界を引く合図が取れない。このホストでは compact 前の未精製範囲が圧力に残り続け、応じられない依頼をセッション終了まで繰り返す。占有 1.2% と、抑止機構を載せない決定（Risks / rollback）を踏まえ、**v0.34 では受け入れる**。実害（ユーザー作業の阻害）が観測されたらパッチで対処する。要約自体は次の規則で Antigravity でも作られる。

**取り残しの発見は marker に依存しない.** orphan range の第一の情報源は marker ではなく `session_refinements.covers_to` である。セッションが終了していて `covers_to` の先に素材が残っていれば、その範囲は定義上 orphan — 畳めるエージェントがもういない。gc はこの差分から直接発見できるので、compact hook の有無に関わらず機械要約は作られる。compact marker はこれを**セッション途中で前倒しする**ためのものであり、長いセッションで本文を早く捨てられるようにする最適化にすぎない。Antigravity では前倒しが効かず、発見がセッション終了まで遅れる。

境界条件を 2 つ決めておく。**一度も畳まなかったセッション**には `covers_to` が無いので、差分の起点はセッション最古のイベントになる（セッション全体が orphan）。**終了イベントが来なかったセッション**は既存の stale 判定（`--stale-after`）で終了とみなす。そうしないと、落ちたエージェントのセッションが永遠に「まだ畳めるかもしれない」状態で本文を抱え続ける。

**「参照時に畳む」は撤回**（2026-08-08）。要約の執筆には LLM が要り、Traceary は LLM を呼ばない。畳みは呼び出し元が LLM だと分かっている経路からしか**依頼**できない。`traceary show` は人間が走らせうるので畳めない。残るのは consolidation トリガーが既に所有している stop hook だけである。参照経路を第二の依頼面にすると、第一経路が既に届く素材に対する面が増え、読み取り経路が書き込みになる。そして compact 後の範囲は参照経路から依頼しても畳めない（素材が文脈にない）ので、第二の面を足しても取り残しは減らない。

これで「畳んでいないから本文を捨てられない」詰まりも解消する。

---

## 着地目標

**単位を改訂**（2026-08-11、#1620）。`GiB` / `GiB/月` はゲートの単位として撤回した。`GiB/月 = 記録単位あたりのバイト数 × 月あたりの作業量` であり、後者は利用者の使い方であって Traceary の性質ではない。per-*event* も撤回した（1 件の作業を何 event として発行するかを Traceary 自身が決めるため、細かく割れば数値が改善してしまう）。

分母は **canonical operation**（`command_executed` 1 件、または `prompt` 1 件）= 利用者の作業単位。計測 30 日窓で 228,595 件、ストア全体で 320,091 件。

| | 単位 | 目標 | 実測 (2026-08-11) |
|---|---|---|---|
| 破棄不能な増分 | bytes / canonical operation | **≤ 5 KiB** | 3.51 KiB |
| うちコマンド記録 | bytes / command execution | **≤ 4 KiB** | 3.06 KiB |
| うちプロンプト記録 | bytes / user turn | **≤ 12 KiB** | 7.86 KiB |
| セッション層の係数（O(corpus) 項） | bytes / session | **≤ 64 KiB** | 41.7 KiB |
| 常駐サイズ（全レバー適用後） | bytes / canonical operation | **≤ 13 KiB** | 9.40 KiB |
| event 発行増幅 | events / canonical operation | **≤ 2.0** | 1.31 |
| ストア全体の増幅 | 常駐 bytes / **重複を除いた**保持テキスト bytes | **≤ 3x** | 2.55x |
| recent 索引の増幅 | 索引 bytes / recent ソーステキスト bytes | **≤ 4x** | 3.37x |
| rebuild 中のピーク | ピーク bytes / 定常 bytes | **≤ 1.5x** | **2.30x — 未達**（#1796） |
| recent 索引層 | 構造 | ビルドのどの時点でも設定した上限で bounded | 未達（#1796） |
| `events.body` 重複率 | 比率 | **< 5%** | — |
| セッション要約 | 比率 | **畳む価値のあるセッションの 95%** | — |
| 起床時注入 | 真偽 | 全ホストで動作、注入量に上限 | — |

閾値は容量の約束ではなく**退行検知のトリップワイヤ**として公開する。マージンは一律ではない: バイト量の行は実測の **1.3〜1.55x**、増幅比の 2 行は既に正規化済みなので **1.18〜1.19x** と狭い。**rebuild ピークだけは例外**で、実測 (2.30x) を**下回る**目標を置いている — 現状で未達だと明示するための行であり、マージンではない。絶対バイト数を正当化する導出は存在しないので、選んだ値だと明記する方が誠実である。GiB 表記（`約 4 GiB`、`約 0.8 GiB/月`）は必ずコーパスを併記した**参考ワークロード例**とし、上限として書かない。recent 索引の外側のストアは bounded ではなく「増加が遅い unbounded」であり、リリースノートにそう書く。

内訳: 重複解消 -2.9 / 索引 2 層化 -13.8 / metadata projection 廃止 -0.47 / output 分離 -1.4 / 圧縮で残りを約半分（いずれも参考コーパス上の GiB。ゲートではない）。

---

### Behavior tests

| Behavior | Given | When | Then | Level |
|---|---|---|---|---|
| 重複を記録しない | Kimi の `wire.jsonl` が進んでいない | stop が 3 回発火 | transcript は 1 行 | integration |
| ターンが進めば記録する | `wire.jsonl` に 3 ターン分 | 各ターン後に stop | transcript は 3 行、本文は互いに異なる | integration |
| 同一ターンが伸びたら記録する | 同じ turnId にブロックが追記される | stop → 追記 → stop | transcript は 2 行、後の行に追記分が含まれる | integration |
| 記録失敗は再試行される | event store がエラー | stop → 復旧 → stop | 2 回目で記録される（marker を書かない） | unit |
| 冪等判定は作業を止めない | 冪等状態が読めない / lock が取れない | stop | exit 0（無防備に記録する。落とさない） | unit |
| 圧力の機械判断 | 未精製 63 KiB のセッション | stop | exit 0（要求しない） | unit |
| 閾値超過で要求 | 未精製 65 KiB | stop | exit 2 / decision:block、理由に対象セッションが載る | unit |
| 判定失敗は作業を止めない | DB がロック中 | stop | exit 0 | unit |
| 再まとめ | 既存要約 gen=1 と新素材 | refine | gen=2、covers が両方を含む、要約は 1 行 | unit |
| 冪等 | 同じ範囲を 2 回 refine | 2 回目 | 要約は 1 つのまま | unit |
| 起床注入 | 要約が 3 件ある | SessionStart | additionalContext に要約のみ。生本文を含まない | integration |
| 要約なしは無音 | 要約 0 件 | SessionStart | 何も出力しない | unit |
| Kimi の注入先 | Kimi ホスト | SessionStart | 何も出さない。初回 UserPromptSubmit で出す | integration |
| Antigravity の注入先 | Antigravity ホスト | PreInvocation | **`injectSteps.ephemeralMessage` で実装済み（#1714）** | integration |
| 索引退避で本文は残る | 窓外の event | projection rebuild | 全文検索に出ない。`show` では本文が読める | integration |
| まだ畳めるうちは捨てない | 継続中で stale でないセッションの未精製 transcript | gc | 本文が残る | unit |
| 終了済みなら畳めないので捨てる | 終了済み・要約なし（`covers_to` 無し）のセッション | gc | セッション全体が orphan になり `degraded=1` 要約後に本文が捨てられる | unit |
| stale も終了として扱う | 終了イベントが来ないまま `--stale-after` を超えたセッション | gc | 終了済みと同じく orphan として扱われる | unit |
| 畳んだら捨てる | 精製済みの transcript | gc | 本文が消え、`body_state=discarded`、`body_bytes` は保持 | unit |
| attested は捨てない | 精製済みセッションの prompt | gc | 本文が残り `body_state=compressed` | unit |
| 統計は本文に依存しない | 全本文 discarded | `report` | 件数・バイト数・時刻が現状と一致 | integration |
| 予算の意味 | 索引予算 2 GiB | rebuild | 索引ファミリ合計が 2 GiB 以下 | integration |
| 取り残しの機械要約 | orphan 範囲あり、LLM 未実行 | gc | `degraded=1` の要約が作られ、本文が捨てられる | unit |
| 閾値超の取り残しも要約できる | orphan 範囲が閾値の 16 倍（1 MiB） | gc | 入力量で失敗せず `degraded=1` の要約が作られる | unit |
| marker 無しでも取り残しを見つける | 終了済みセッション、`covers_to` の先に素材あり、orphan range は未記録 | gc | 差分から orphan を発見し `degraded=1` 要約を作る | unit |
| compact で圧力がリセットされる | 未精製 65 KiB のまま compact | compact → 直後の stop | 旧範囲は orphan として記録され、stop は compact 後の素材だけで判定する（未精製 0 なら exit 0） | unit |
| 注入予算は閾値と独立 | 同じ要約集合・同じ注入予算 8 KiB で、精製閾値だけを 64 KiB と 1 MiB に変えた 2 条件 | SessionStart | 2 条件の出力が完全に一致し、どちらも 8 KiB 以下 | unit |

---

### TDD plan

| Behavior | Red | Green | Refactor target |
|---|---|---|---|
| 重複記録 | 進んでいない `wire.jsonl` に stop 3 回で 3 行出る | `(session, wire turn ID, 本文 fingerprint)` の冪等化 | `hook_kimi_transcript.go` / hook dedup |
| 既存重複の修復 | apply が全 246,657 行を 1 トランザクションで読む | クラスタ単位のバッチ化 + 再開 + purge + `--list-runs` | `content_event_dedupe_datasource.go` |
| 圧力判定 | 閾値前後で終了コードが変わらない | 圧力計算 + 判定 | `hook_runtime.go` stop 分岐 |
| 再まとめ | 2 回目の refine が要約を上書きする | 世代 + 被覆範囲 | `session_refinements` usecase |
| 起床注入 | 出力が空のまま | additionalContext 生成 | 新規 `wake_injection` |
| 本文分離 | body が events 列にある | 移送 migration + 読み取り経路 | `infrastructure/sqlite` |
| 索引 2 層 | 全期間に trigram が張られる | 要約層を read 経路に接続 | `search_projection_*` |
| gc 状態機械 | 本文が消えない | 状態遷移 | `store gc` |

---

### Risks / rollback

**手続き化リスク.** 「精製」を 1 つの巨大 usecase にしないこと。*圧力の判定* / *要約の保存* / *索引の退避* / *本文の破棄* は変更理由が違う。混ぜると gc がすべてを知る神クラスになる。

**premature abstraction リスク.** ホスト差は実測で **3 点だけ**で、しかも 2 種類しかない — 起床注入点（Kimi は初回 UserPromptSubmit、Antigravity は PreInvocation、他は SessionStart）と compact の観測可否（Antigravity のみ不可）。いずれも既存の hook 配線で表現できるので、ホストごとの Strategy を作らない。

**migration / compatibility.**

- 本文移送は 5 GiB の書き換えになる。**バッチ + チェックポイント**で行い、中断可能にする。`store payload-rehearsal` の既存機構を再利用する
- `event_metadata_projection` の廃止は読み取り経路の切り替えが先。同一 minor 内で二段階（並行運用 → 撤去）
- 索引の作り直しは既存の generation 機構に載せる（新規機構を作らない）
- 本番 DB での検証は禁止。`ref_live_db_migration_gap` のとおり、未リリース migration は巻き戻せない

**rollback trigger.**

- 本文移送後に `report` の統計が移送前と一致しない → 移送を中止し generation を破棄
- 起床注入で 1 セッションあたり注入量が予算を超える → 注入を無効化（記録は継続）
- stop hook の判定がターンを止める（exit 2 の暴発）→ 閾値を無限大にして無効化できること

**stop トリガーの runaway 抑止は載せない**（2026-08-08 決定）. stop トリガーは**繰り返し要求の抑止機構なし**で出荷する。エージェントが依頼を無視しても圧力は閾値上に残り、次の stop でもう一度依頼する。compact hook を持つホストでは繰り返しは compact までで、そこを跨いだ範囲は圧力ごと gc へ渡す（取り残し範囲を参照）。compact hook が無い Antigravity では境界が引けないので、セッション終了まで繰り返す。

根拠: 現状 Traceary の hook はどの経路も非ゼロ終了を返さない（すべて fail-open）。今回がユーザーのターンに干渉する初めてのケースで、ホスト側の continuation 挙動はここでまだ実測されていない。観測前に anti-runaway を積むのは、存在しないかもしれない失敗モード向けの実装になる。緩和は v0.34 タグ前の dogfood とし、ユーザー作業を中断するようなら閾値で無効化してパッチリリースする。依頼が無視されたときに失うのは「なぜ」だけである: 未畳み範囲そのものは記録されたまま残り、gc の機械要約が「いつ・何を・何回・どのコマンド」を拾う。落ちるのは `degraded=1` の経路なので、エージェントの判断理由は復元できない。この情報損失込みで v0.34 は受容する — 抑止機構を先に積むより、実際に無視されるのかを dogfood で観測する方が安い。

---

## Resolved decisions

| # | 決定 | 根拠 |
|---|---|---|
| D1 | 索引予算は**索引ファミリ合計バイト**で表現。既定 2 GiB | 現行 `recent_byte_limit` は入力本文を測っており 2 GiB 指定が 6.35 GiB になる |
| D2 | 固定化のトリガーは **stop hook のみ** | compact hook を持つホストでは観測専用、Antigravity には hook 自体が無い（一次情報確認済み） |
| D3 | attested は **人間の指示** と **コマンドの同一性**。`output_text` は含めない | 「何を実行したか」の証明に出力は不要。1.47 GiB / 6,153 B/row |
| D4 | 閾値 **64 KiB**（stop で提示する素材量の設計目標。上限ではない） | 提示時 16k トークン。gc 機械要約と起床注入予算は有界入力を仮定しない |
| D5 | 起床時は**完成済み要約のみ**注入。生ログは渡さない | 素材を渡すとセッション全体を汚染する |
| D6 | skeleton と `body_bytes` は**永久**。本文を捨てても統計は出せる | 「本文はなくてもやりとりの時間はほしい」 |
| D7 | セッション要約は**永久**。本文の約 1/500 | 要約が残ることが本文を捨てられる条件 |
| D8 | 重複記録の修正を**最優先**に置く | 57.7% が重複。閾値も索引予算もこの上に載っている |
| D9 | attestation chain (#1677/#1678) は **v0.35 以降** | 二本柱に直接寄与しない。「小さく」の方針 |
| D10 | ~~`cockpit` / `tui` / `dashboard` は **v0.34 で削除**~~ → **D13 で改訂** | 常時閲覧系は二本柱に寄与しない（製品判断） |
| D11 | `top` は v0.34 告知 / v0.35 削除 | `docs/cli-stability.md:52` の permanent 公約の撤回が先 |
| D12 | compact 到達時に未精製範囲を stop 圧力から**切り離して gc 所有へ移し、圧力をリセット**する。compact hook を持たない Antigravity は対象外（残存リスクを受け入れ） | compact 後の素材は文脈にないので、以後 stop を繰り返しても畳めない |
| D13 | 対話 surface は `memory inbox review` **だけ**残す。cockpit・`sessions` live dashboard・bare `traceary` の TTY 既定は **v0.34 告知 / v0.35 削除**。削除後の bare `traceary` は TTY でも help | D10 は「即削除」を前提にしていたが、`docs/cli-stability.md` に公約が無いだけで README / docs/interactive が `tui` を stable と明記していた。`top` (D11) と同じ扱いに揃える |
| D14 | 「backing table が空」を根拠にした退役は、**述語全体**と**deprecation 契約**の両方を満たす場合に限る | Wave 4 spike で 4 件中 2 件が脱落した。`list --failures` は `failed = 1` で 1,943 件ヒットする（exit code だけを見た evidence が誤り）。`store workspace-alias` は canonical replacement が無く、`docs/cli-stability.md:98-115` の notice 形式を満たせない（この後半の根拠は **D15 で無効化**。no-replacement 形態が追加されたので、残る根拠は alias が現役であることだけ） |
| D15 | 告知は **command / behaviour / no-replacement** の 3 形態を 1 実装・1 stderr 経路で出す。notice は **run step** に付ける（pre-run hook ではない）。help / usage text は stdout byte-identity 保証の**対象外** | Wave 4 実装で判明。cobra は `--help`・引数エラー・required flag 検証を RunE より前に処理するので、pre-run hook では実行されない invocation にも notice が出る（`memory admin graph add` で実測）。逆に「実行時のみ告知」にすると `--help` が何も言わなくなるため、`Short` / `Long` に非推奨を書くことを義務化した。それは親の command listing を変えるので、help を byte-identity に含めたままだと流儀自体が自己矛盾する |

### D13 — 対話 surface を一本に絞る（Wave 4 spike 2026-08-10）

コマンド名の事実確認から始まった。`traceary cockpit` は存在せず、`presentation/cli/cockpit.go:63-64` は `Use: "tui"` / `Aliases: ["dashboard"]` である。D10 が「cli-stability.md に公約が無いので即削除できる」と書いたのは、その 1 ファイルの中では正しい。しかし `README.md:60,187`、`docs/interactive/README.md:137`、`docs/cli/README.md:100` は `tui` を "stable explicit entrypoint" / "compatibility path" と明記していた。公約の所在がファイル 1 枚に閉じていない以上、D11 と同じ「撤回 → 1 minor 告知 → 削除」に揃える。

`sessions` の live dashboard も同じ理由（常時閲覧系は二本柱に寄与しない）で退役するが、**別 issue に分ける**。cockpit は `cockpit.go` と root dispatch、`sessions` は `top.go` の dashboard runner で、実装も rollback 単位も別だからである。gpt-5.6-sol の指摘を採用した。

削除が script に対して無害である根拠は、両方とも既に非 TTY 経路で目的の挙動を持っていること:

- `presentation/cli/root.go:529-547` — bare `traceary` は対話 pair のときだけ cockpit を開き、それ以外は `cmd.Help()`
- `presentation/cli/top.go:727-738` — bare `sessions` は非 TTY で `writeTopSnapshotText` に落ちる。`--snapshot` と同じ writer・同じ loader・同じ pane cap

つまり削除は分岐を 1 本消す作業であり、新しい挙動を発明しない。

告知の形式には仕様の穴がある。`docs/cli-stability.md:92-116` は deprecated *command*、`:129` は deprecated *flag* の形式しか定義していないが、bare `traceary` と `sessions` は**コマンドが残ったまま既定の描画だけが変わる**。「このコマンドは非推奨です」は嘘になるので、同じ 1 形式のまま主語を behaviour にする 1 文を #1687 で追記する。

注意点として、`traceary top` は `sessions` と同じ live runner を通る（`presentation/cli/top.go:168-176,229`）。#1688 の `top` notice と #1765 の mode notice が両方出ると `docs/cli-stability.md:115` の「1 invocation につき 1 行」に違反する。`top` notice を優先し、後に着地した側がテストで固定する。

計測は根拠ではなく文脈として残す: 記録済み shell audit 282,710 件のうち `traceary ` で始まるのは 54 件で、`sessions` / `top` / `tui` の起動は **0 件**。ただしこれは agent 経由の shell しか見ていないので、人間が自分の端末で叩いた分は観測できない。退役の根拠は二本柱との不整合と製品判断に置く。

### D14 — 「空テーブル」基準の適用範囲（Wave 4 spike 2026-08-10）

#1689 は「backing table が 1 行も持ったことがない」を客観基準として 4 件を挙げたが、実測で 2 件が脱落した。

`list --failures` の述語は exit code 専用ではない（`infrastructure/sqlite/sql/select_recent_events.sql:20`）。`ca.failed = 1` が 1,943 件ヒットし、うち `host_error` 889 件は 2026-07-24 から今日まで書かれ続けている。`failed=1 / exit_code=NULL` は `infrastructure/sqlite/command_audit_store_test.go:426-430,512-529` で意図された契約として固定されており、事故ではない。→ #1767。

`store workspace-alias` は table こそ 0 行だが、deprecation 契約そのものが通せない。`docs/cli-stability.md:98-115` は notice に canonical replacement を必ず名指しさせるが、alias の代替は存在しない。加えて alias は死んだデータではなく、report での再分類（`workspace_identity_datasource.go:100-121`）、conflict sample の抑制（`:202-217`）、将来 observation の直接 `explicit_alias` 化（`event_delivery_store.go:400-422`）に効いている。→ #1768。

その #1768 で判明したこと: conflict 14,684 行は distinct `(session_id, workspace)` にすると **147 組**（117 session / 36 workspace）にすぎない。`codex` / `post_tool_use` だけで 13,664 行を 16 組から出している。「手作業では捌けない量」という当初の見立ては誤りで、実際には捌ける量を、行数で数えているせいで捌けない量に見せていた。report が組数ではなく行数を出していること自体が defect である。

### D15 — 告知の形式と発火点（Wave 4 実装 2026-08-10）

Wave 4 は 4 issue とも「v0.34 では何も消さない、告知だけ」で着地した。実装 gpt-5.6-luna、review Claude Opus 5（主）+ gpt-5.6-sol（独立第二）。

| issue | PR | 告知対象 | 形態 |
|---|---|---|---|
| #1688 | #1769 | `traceary top` | command |
| #1687 | #1770 | `tui` / `dashboard` と bare `traceary` の TTY 既定 | command + behaviour |
| #1689 | #1772 | `memory admin graph add` / `graph list`、`session label` 一式 | no-replacement |
| #1765 | #1773 | `sessions` の対話的既定描画 | behaviour |

削除は v0.35（#1764 / #1766 / #1690 / #1691）。

**3 形態は 1 実装に集約した。** `presentation/cli/deprecation.go` の `writeDeprecationNotice` が唯一の stderr 経路で、`replacement == noReplacement` のときだけ「置き換え先はありません」に切り替わる。command 形態だけが `applyCommandDeprecation` で hook を挿し、behaviour 形態は挙動が起きる直前に直接呼ぶ。当初 luna が置いた `applyBehaviorDeprecation` は素通しの wrapper で、`apply*` という名前が hook 設置を示唆するのに即時書き込みだったため削除した。

**no-replacement 形態の追加は D14 の根拠を一つ無効化する。** D14 は `store workspace-alias` が退役できない理由として「canonical replacement が無いので deprecation 契約そのものを通せない」（`.ai/spec/1606.md:416`）を挙げたが、`docs/cli-stability.md:123` が後継なしの告知を明示的に許した以上、この根拠はもう成立しない。残るのは #1768 が実際に測った方——alias は死んだデータではなく、report の再分類・conflict sample の抑制・将来 observation の `explicit_alias` 化に効いている——だけである。v0.35 の退役判定は「後継が有るか」ではなく「失われるものが無いことを示せるか」で分岐させる。

**発火点は run step。** `PreRunE` に置いた初期実装が誤りだったことは #1689 の review で sol が実測した: cobra v1.10.2 は `command.go:1007` の `ValidateRequiredFlags` を `PreRunE`（`:999`）の**後**に走らせるので、`traceary memory admin graph add memory-a`（`--to` / `--relation` 欠落）は notice を出してから exit 1 する。`--help`（`:934`）と `ValidateArgs`（`:968`）も同様に前段で短絡する。私は #1688 の review でこの順序を見ていながら「`top` には required flag が無い」で流しており、`graph add` には有る。RunE 差し替えに変えると、この 3 つの短絡すべてに対して「実行されたときだけ出る」が真になる。回帰テストは旧実装で落ちることを確認してから入れた。

**その代償として help text を byte-identity 保証から外した。** 実行時のみ告知にすると `--help` が非推奨を伝えなくなるので、`Short` / `Long` に非推奨と削除目標を書くことを義務化した。この除外は sol の #1770 HIGH（`tui` の `Short` 変更で bare 非 TTY stdout が 2114 → 2161 バイトに動いた）から出ている。コードを戻すのではなく `docs/cli-stability.md` 側に「help / usage は保証の対象外、automation は `--help` を parse しない」を明記して解いた。詳細は下の表。

**1 invocation 1 notice は `top` で危うかった。** `top` と `sessions` は同じ live runner を通るので、#1765 の mode notice をそのまま出すと `top` の command notice と 2 行になる。`interactiveDashboardNoticeApplies(commandName) bool` という名前付き述語に理由コメントごと閉じ込め、v0.35 で `top` が消えるまで削除されないようにした。sol が実 PTY で確認: `sessions` 1 行 / `top` 1 行（command notice のみ）/ `top --snapshot` 1 行 / `sessions --snapshot` 0 行 / 非 TTY `sessions` 0 行。制御シーケンス順は `notice → ?1049h → paint → ?1049l` で、alt-screen に飲まれず、復帰後の画面に残る。

**sol の HIGH は 4 件で、2 件を採用、2 件を却下した。**

| PR | 指摘 | 扱い |
|---|---|---|
| #1769 | `--help` / 引数エラーで notice が出ない | **採用（別解）**。cobra の hook では直せないので、境界を policy に明記し test で固定した |
| #1769 | CHANGELOG が今も permanent と書いている | 却下 |
| #1770 | `tui` の `Short` 変更で bare 非 TTY stdout が動いた | 却下。ただし policy 矛盾を露出させ、help 除外規則を生んだ |
| #1772 | required flag 欠落時に notice が出て失敗する | **採用**。上の run step 変更 |

却下した CHANGELOG の件は、当該 2 行が `## [v0.19.0] - 2026-05-26` 節（Sessions dashboard #1083 と bare CLI #1060 のエントリ）の中にあるためである。公開済み release notes と対になる不変記録なので、書き換えは公約の撤回ではなく記録の改竄になる。行番号は Wave 4 自身の CHANGELOG 追記で動くため、ここには残さない。

`tui` の件を「却下」と書くのは結論部分だけの話である。計測（2114 → 2161 バイト）は正しく、当時の policy に照らせば違反でもあった。`Short` 変更が契約違反ならこの流儀ではどの subcommand も非推奨にできないので、コードではなく policy を直した。指摘が無ければ矛盾は残っていた。

副産物として #1771（`memory_usecase_hygiene_test.go:644` の `MaxDuration: time.Millisecond` に依存した flaky test）を起票した。PR #1770 の CI 赤の原因で、branch は当該ファイルに触れていない。
