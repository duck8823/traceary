# #1819 — verify recent_source_bytes

Decision: option 3. `store search-projection status` reports cache vs
SUM(decoded_bytes) for state.generation_id. No triggers, no rewrite.
Public: `docs/research/recent-source-bytes-verifier.md`.
