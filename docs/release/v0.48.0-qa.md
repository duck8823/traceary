# v0.48.0 release QA

[日本語](./v0.48.0-qa.ja.md)

## Scope

v0.48.0 ships store-capacity reclaim, compact coverage, consolidation delivery,
and the projection-completion gate. It includes every merged #2263 child except
the documented #2266 no-go. It does not add a store subcommand. It does not
publish event bodies, prompts, transcripts, or projection rows.

## Candidate checks

Release preparation at `61a58e35` passed:

```sh
go run ./cmd/repo-tooling release verify-changelog
python3 scripts/verify_release_manifests.py
go run ./cmd/repo-tooling integrations verify
go run ./cmd/repo-tooling docs verify-landing
go run ./cmd/repo-tooling docs verify-i18n
go test ./...
go tool golangci-lint run
```

The bump is the 11-file minor pattern: `VERSION`, both changelogs, landing
`index.html` eyebrow + `components.jsx`, and the six host manifests. CI on
PR #2291 is green. `Formula/traceary.rb` is GoReleaser-owned and is not in
this PR.

## Release-gate measurements (RC `aabaaa47`, copy only)

Copy: `/tmp/traceary-2269-gate.GaBp9U/traceary-copy.db` (reused; no new
`sqlite3 .backup`). Live store was not opened by a repo binary.

1. **Compact.** Replica plan refused: need 32,246,366,208 bytes, have
   ~22–23 GiB (`insufficient free space`, estimated reclaimable 973,533,184).
   In-place fallback also returned that error (the CLI wraps the replica
   refusal; `compact_strategy` JSON was not produced). Copy size stayed
   15,355,412,480 bytes. `--force` was not passed.
2. **Projection rebuild.** `scripts/verify-projection-completion.sh`
   `--min-free-ratio 1.4` (measured free/copy ≈ 1.53; default 3 would exit 64).
   Result **FAIL** at iteration 61: `inspect projection before resume: failed
   to ping read-only SQLite DB: context deadline exceeded`. Wall 2,999 s.
   Family bytes 8,427,220,992 versus target 1,535,115,264 (5.49×):
   keywords 6,105,280,512, fingerprints 2,105,118,720. Copy grew to
   18,077,872,128 bytes. On-main JSON
   `docs/release/v0.48.0-projection-completion-evidence.json` is a prior FAIL
   on a different 20.6 GiB copy.
3. **Live capture.** `scripts/verify-post-upgrade-live-capture.sh --skip
   gemini='IneligibleTierError'` with RC `aabaaa47` stamped
   `-X main.version=0.47.1` so identity matches installed 0.47.1 packages
   (unsigned `dev` fails the identity gate). PASS claude / codex /
   antigravity / grok / kimi (`session_started=1 prompt=1`). SKIP gemini.
4. **Doctor.** `--json --warnings-ok` exit 0, mode
   `metadata_only_large_store`, 0 FAIL, 16 WARN (including plugin-version
   mismatch against unsigned `dev`, `store-size`, `search-projection-generation`).
   `consolidation-conversion` is not emitted in this mode.

## #2266

No-go. The completion gate does not reach `state=complete` on an operator-store
copy. No `WITHOUT ROWID` conversion ships. The follow-up docs PR closes #2266
on that evidence (in-repo `v0.48.0-projection-completion-evidence.json` and the
copy run that FAILed at iteration 61 with a read-only ping timeout).

## Artifact and Formula ownership

`Formula/traceary.rb` is generated from tagged artifacts by the release
workflow. This PR does not edit it.

## Parent issue

#2263 stays open until the tagged release workflow closes it.
