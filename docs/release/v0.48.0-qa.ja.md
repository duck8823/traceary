# v0.48.0 release QA

[English](./v0.48.0-qa.md)

## 対象

v0.48.0 は store 容量の回収、compact の被覆、consolidation の配送、projection
完了ゲートを出荷します。merge 済みの #2263 子 Issue を含み、計測済み no-go の
#2266 だけ除きます。store サブコマンドは増やしません。event body、prompt、
transcript、projection 行は公開しません。

## Candidate check

release 準備 `61a58e35` では次が通りました。

```sh
go run ./cmd/repo-tooling release verify-changelog
python3 scripts/verify_release_manifests.py
go run ./cmd/repo-tooling integrations verify
go run ./cmd/repo-tooling docs verify-landing
go run ./cmd/repo-tooling docs verify-i18n
go test ./...
go tool golangci-lint run
```

bump は minor の 11 ファイルです（`VERSION`、changelog 両方、landing の
`index.html` eyebrow と `components.jsx`、6 つの host manifest）。PR #2291 の
CI は green です。`Formula/traceary.rb` は GoReleaser 所有で、この PR には
含めません。

## release gate 計測（RC `aabaaa47`、copy のみ）

copy: `/tmp/traceary-2269-gate.GaBp9U/traceary-copy.db`（再利用。新規
`sqlite3 .backup` は取っていない）。repo binary は live store を開いていません。

1. **Compact.** replica 計画は拒否。必要 32,246,366,208 bytes、空きおよそ
   22–23 GiB（`insufficient free space`、estimated reclaimable 973,533,184）。
   in-place fallback も同じエラーを返した（CLI が replica 拒否を wrap し、
   `compact_strategy` JSON は出ていない）。copy サイズは 15,355,412,480 bytes
   のまま。`--force` は渡していない。
2. **Projection rebuild.** `scripts/verify-projection-completion.sh`
   `--min-free-ratio 1.4`（実測 free/copy ≈ 1.53。既定 3 だと exit 64）。
   結果は **FAIL**（iteration 61: `inspect projection before resume: failed
   to ping read-only SQLite DB: context deadline exceeded`）。wall 2,999 s。
   family bytes 8,427,220,992 / 目標 1,535,115,264（5.49 倍）。keywords
   6,105,280,512、fingerprints 2,105,118,720。copy は 18,077,872,128 bytes
   まで増えた。main 上の
   `docs/release/v0.48.0-projection-completion-evidence.json` は別の
   20.6 GiB copy での先行 FAIL。
3. **Live capture.** `scripts/verify-post-upgrade-live-capture.sh --skip
   gemini='IneligibleTierError'`。RC `aabaaa47` に
   `-X main.version=0.47.1` を付け、導入済み 0.47.1 package と identity を
   揃えた（unsigned の `dev` は identity gate で落ちる）。PASS claude /
   codex / antigravity / grok / kimi（`session_started=1 prompt=1`）。
   SKIP gemini。
4. **Doctor.** `--json --warnings-ok` exit 0。mode は
   `metadata_only_large_store`。FAIL 0、WARN 16（unsigned `dev` に対する
   plugin-version 不一致、`store-size`、`search-projection-generation` など）。
   この mode では `consolidation-conversion` は出ない。

## #2266

no-go。完了ゲートはオペレータ store copy で `state=complete` に達しない。
`WITHOUT ROWID` 変換は出荷しない。#2266 は open のまま。

## Artifact と Formula の所有者

`Formula/traceary.rb` は tagged artifact から release workflow が生成します。
この PR では編集しません。

## 親 Issue

#2263 は tagged release workflow が閉じるまで open のままです。
